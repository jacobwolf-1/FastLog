# Xcode build artifacts and per-user state — never commit these.
build/
DerivedData/
*.xcuserstate
**/xcuserdata/
.DS_Store

# Local Supabase credentials — copy FastLogConfig.example.plist and fill in.
FastLog/FastLogConfig.plist
