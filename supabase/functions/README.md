# Supabase Edge Functions

No Edge Functions are implemented in this first pass.

Recommended next functions or route handlers:

- `food-logs`
- `dashboard`
- `targets`
- `saved-meals`
- `weight-entries`

Keep all functions authenticated. Public unauthenticated writes should not exist for FastLog health and nutrition data.
